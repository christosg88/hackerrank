#include <iostream>

int compute_power_sum(int val, int power);

int main() {
    int val, power;
    std::cin >> val >> power;

    std::cout << compute_power_sum(val, power);

    return 0;
}

int compute_power_sum(int val, int power) {
    if
}
